.venv
__pycache__/
db.sqlite3
.DS_Store
.env
DJANGO_CHEATSHEET.md
PRODUCTION_SECURITY_SUMMARY.md
RAILWAY_DEPLOYMENT.md

# AI tooling / local-only
.gstack/
.claude/
.superpowers/
.next/
graphify-out/
stitch_recommendations_improved/

# User uploads (served by Django, not git)
/media/

# Collected static files are generated by collectstatic.
/staticfiles/*
!/staticfiles/.gitkeep

# Tester/user feedback write-ups. These quote real people and carry their
# names, emails and private messages; this repo is public. Keep them outside
# the working tree (e.g. ~/Desktop/<name>-feedback-<source>/) and reference
# only the resulting code changes here.
*_FEEDBACK_*.md
*-feedback-*.md
*_feedback_*.md
